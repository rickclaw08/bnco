# Daily Memory - March 30, 2026 (Monday)

## Carry-Over from March 29
- GHL export complete at `ghl-export/` (100 contacts, 100 convos, 14 files). Ready for migration import.
- VAPI inbound routing fix pending Brand's decision (Chad/West Shore Home callback issue)
- Judith Johnson (#1): landline (513) 299-2669, no outreach yet
- Faith Hudson (#2): bridge via daughter Brianna (513) 356-9515, unknown if Brand texted
- RealAuction locked out, no new April 8 leads discoverable
- ClawOps: 5 agent fixes + 4 retries deferred
- **8 days to April 8 auction**
- Brand was quiet all weekend (no messages Sat or Sun)

## Morning Session (9:19 AM - 9:41 AM)

### OpenClaw Update
- Brand requested self-update. Updated to OpenClaw 2026.3.28 (f9b1079) via npm. Gateway restarted.

### QMD Memory Backend Switch
- Brand requested switching memory from semantic (default) to QMD
- Installed Bun v1.3.11, installed QMD globally via `bun add -g @tobilu/qmd`
- Initial build failed (missing @types/node, 400+ TypeScript errors)
- Fixed: ran `bun install` in QMD package dir, then `bun run build` succeeded
- QMD CLI confirmed working (`qmd --help` returns usage)
- Config updated in `~/.openclaw/openclaw.json`: `memory.backend = "qmd"`, search mode BM25, 5-min reindex interval, 6 max results, 4s timeout
- Gateway restarted. QMD confirmed live (memory_search returns `"provider": "qmd"`)

### QMD Explanation to Brand
- Brand asked what QMD does for memory. Explained:
  - QMD = BM25 keyword matching + local vector search + reranking (hybrid)
  - vs. old semantic = OpenAI embedding API calls, vector-only similarity
  - QMD stronger at exact matches (case numbers, phone numbers, addresses, IDs)
  - Semantic stronger at paraphrasing/conceptual queries
  - QMD runs locally (no external API calls for embeddings)
- Brand asked if semantic hallucinated more. Clarified: search doesn't cause hallucination, but bad retrieval gives worse context, which makes model more likely to guess wrong. QMD fixes retrieval, not hallucination directly.
- Brand asked if both QMD and LCM (Lossless Claw) are active. Confirmed both running:
  - QMD = searches written memory files (MEMORY.md + daily notes)
  - LCM = searches compacted conversation history (SQLite archive)
  - Demonstrated both with Johnson/Starcrest queries

### Project Velocity Status Update
- Brand asked for last Velocity activity summary. Pulled via LCM expand_query.
- Confirmed: last real activity was Mar 27. 2 leads (Johnson, Hudson), 9 days to auction, zero confirmed interest, all search tools blocked.

### Faith Hudson Skip-Trace Correction
- **Brand called (513) 356-9515 - confirmed it's FAITH's number, NOT daughter Brianna's.** Skip-trace data was wrong.
- Faith's landline (513) 751-3557 also dead (already known)
- Brand texted (513) 356-9515, no response
- **MEMORY.md updated:** Removed Brianna bridge contact info, corrected to show 356-9515 as Faith's number
- **Remaining Hudson contact paths:** Son Nehemiah (513) 550-9812, emails (hudson.faith@ymail.com, israel.1faith@yahoo.com), door knock

### Current Status
- Johnson: untouched, Brand hasn't tried landline yet
- Hudson: two numbers dead, text ignored, Nehemiah and email untried
- 9 days to auction (April 8)
- No new leads, all search tools still blocked
